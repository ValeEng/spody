# PyInstaller bundle outputs (also covered by root .gitignore)
build/
dist/
# NOTE: do NOT ignore *.spec -- the bundle spec is tracked so the
# build stays reproducible across machines.

# Package metadata
*.egg-info/
